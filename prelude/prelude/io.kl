{
    export debug : forall a . a -> a = io_debug,

    # TODO I should use a proper Result abstraction (or Either abstraction) here

    export read_file : Str -> < Ok : Str, Err : Str > = io_read_file,

    export write_file : Str -> Str -> < Ok : Unit, Err : Str > = fn path =>
        fn contents => (io_write_file { path = path, contents = contents }),

}

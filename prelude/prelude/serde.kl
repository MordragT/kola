{
    export to_json : forall a . a -> < Ok : Str, Err : Str > = serde_to_json,
    export from_json : forall a . (Type a) -> Str -> < Ok : a, Err : Str > = fn proto =>
        fn json => (serde_from_json { proto = proto, json = json }),
}

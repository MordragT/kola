{
    #add_one = fn x => x + 1,

    #main = 15 |> add_one |> add_one |> add_one,
    #main = add_one <| add_one <| add_one <| 15,
    #main = (add_one (add_one (add_one 15))),

    #main = let r = { a = { b = { c = "hello" } } } in r.a.b.c,

    effect type Io = {
        print : Str -> Unit,
        read : Unit -> Str,
    },

    mocked_print = fn x => "mocked",

    effectful = fn x => do print x,

    simple = handle effectful
        | print arg => (mocked_print arg),

    main = (simple "Hello"),
}

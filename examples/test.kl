{
    #simple = { a = { b = 10 } },
    #main = { simple : { a : { b : Num } } | a.b : Num += 20 },
    #main = { simple : { b : Num } | a.b : Num += 20 },
    #main = simple.a,
    #result : { b : Num } = simple.a,
    #main = result,
    #intermediate = simple.a,
    #main = { intermediate : { b : Num } | -b : Num },

    #main = (list_len [1, 2, 3]),

    #id = fn x => x,
    #a = (id 10),
    #b = (id true),
    #main = { a = a, b = b },


    #main = let id = fn x => x in
    #    let a = (id 10) in
    #    let b = (id true) in
    #    { a = a, b = b },


    type Option = forall a . < Some : a, None >,
    #main = (Some 10),

    #main : forall a . Option a = (None ()),

    #main = (Some 10), # make this a not panicing error

    #a = { a = 1, b = 2 },
    #main = { a : { a : Num, b : Num } | +c = 3 | a = 4 },

    #main = let data = [1, 2, 3] in
    #    case data of
    #        [] => "empty",
    #        [x] => "single",
            #[a, b, ...rest] => "multiple",


    type Person = forall a . { name : Str | a },

    alice : forall a . Person a = { name = "Alice", age = 30 },

    main = case alice of
        { name, age } => "hallo", #name,
}
